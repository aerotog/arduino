#ifndef LED_MODE_REPOSITORY
#define LED_MODE_REPOSITORY

#include <Arduino.h>

class LedModeRepository
{
private:    
    /* data */
public:
    LedModeRepository(/* args */);

//    void GetStartupInternal();
//    void SetStartupIn(int mode);

    // ~LedModeRepository();
};

#endif
